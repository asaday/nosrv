name: database-raw-postgres

providers:
  node:
    db:
      provider: postgres
      urlEnv: DATABASE_URL
      appId: database-raw-postgres

  google-functions:
    db:
      urlEnv: DATABASE_URL
      appId: database-raw-postgres

  lambda:
    db:
      urlEnv: DATABASE_URL
      appId: database-raw-postgres
